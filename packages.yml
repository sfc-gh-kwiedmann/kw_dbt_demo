# Uncomment the lines below and run dbt deps to use the Snowflake semantic view materialization
# packages:
#   - package: Snowflake-Labs/dbt_semantic_view
#     version: 1.0.3

packages:
  - local: local_packages/my_custom_pkg